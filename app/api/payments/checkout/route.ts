import { randomUUID } from "crypto";
import { NextRequest, NextResponse } from "next/server";
import { getServerContext } from "../../../../lib/server-auth";
import { getServerSecret } from "../../../../lib/server-secrets";
import { sendExternalCustomerNotice } from "../../../../lib/customer-notifications";
export const runtime = "nodejs";
const money=(v:any)=>new Intl.NumberFormat("id-ID",{style:"currency",currency:"IDR",maximumFractionDigits:0}).format(Number(v||0));
export async function POST(req: NextRequest) {let ctx:any=null; let workspaceId=""; let orderCode="";try {const body=await req.json();workspaceId=String(body.workspace_id||"");const packageId=Number(body.package_id||0);if(!workspaceId||!packageId)return NextResponse.json({ok:false,error:"error, terjadi kesalahan."},{status:400});ctx=await getServerContext(workspaceId);const key=await getServerSecret(ctx.admin,"luma_xendit_secret_key");if(!key)return NextResponse.json({ok:false,error:"error, terjadi kesalahan."},{status:503});const {data:pkg,error:pkgError}=await ctx.admin.from("luma_token_packages").select("id,label,tokens,price,status").eq("id",packageId).single();if(pkgError)throw pkgError;if(pkg.status!=="active"||Number(pkg.price||0)<=0)return NextResponse.json({ok:false,error:"Paket token belum tersedia."},{status:400});orderCode=`TOPUP-${randomUUID().replace(/-/g,"").slice(0,12).toUpperCase()}`;const origin=(process.env.NEXT_PUBLIC_APP_URL||new URL(req.url).origin).replace(/\/$/,"");const expiresAt=new Date(Date.now()+3*24*60*60*1000).toISOString();const customer:any={reference_id:`luma-${ctx.user.id}-${Date.now()}`,type:"INDIVIDUAL",email:ctx.user.email||undefined};const payload:any={reference_id:orderCode,session_type:"PAY",mode:"PAYMENT_LINK",amount:Number(pkg.price),currency:"IDR",country:"ID",locale:"id",expires_at:expiresAt,description:`Lumaway ${pkg.label} - ${pkg.tokens} token`,customer,items:[{reference_id:`token-${pkg.tokens}`,name:`Lumaway ${pkg.tokens} Token`,description:"Lumaway AI token top up",type:"DIGITAL_SERVICE",category:"SOFTWARE",net_unit_amount:Number(pkg.price),quantity:1,currency:"IDR"}],metadata:{workspace_id:workspaceId,user_id:ctx.user.id,package_tokens:String(pkg.tokens)}};if(origin.startsWith("https://")){payload.success_return_url=`${origin}/#billing`;payload.cancel_return_url=`${origin}/#billing`}const response=await fetch("https://api.xendit.co/sessions",{method:"POST",headers:{Authorization:`Basic ${Buffer.from(`${key}:`).toString("base64")}`,"Content-Type":"application/json"},body:JSON.stringify(payload)});const x=await response.json();if(!response.ok)throw new Error(x?.message||x?.error_code||`Xendit error ${response.status}`);const providerExpiry=String(x.expires_at||expiresAt);const {error:orderError}=await ctx.admin.from("luma_topup_orders").insert({workspace_id:workspaceId,user_id:ctx.user.id,order_code:orderCode,package_tokens:pkg.tokens,amount:pkg.price,status:"pending",payment_provider:"Xendit",payment_method:"Secure Checkout",payment_session_id:x.payment_session_id||null,payment_url:x.payment_link_url||null,expires_at:providerExpiry,provider_payload:{status:x.status,allowed_payment_channels:x.allowed_payment_channels||[],requested_expires_at:expiresAt}});if(orderError)throw orderError;await ctx.admin.from("luma_api_usage_events").insert({workspace_id:workspaceId,user_id:ctx.user.id,provider:"xendit",service:"payment_session",request_type:"token_checkout",status:"success",reference:orderCode,metadata:{payment_session_id:x.payment_session_id||null,amount:Number(pkg.price),tokens:Number(pkg.tokens),expires_at:providerExpiry,channels:x.allowed_payment_channels||[]}});const expiryText=new Date(providerExpiry).toLocaleString("id-ID",{timeZone:"Asia/Jakarta",dateStyle:"medium",timeStyle:"short"});const delivery=await sendExternalCustomerNotice(ctx.admin,{userId:ctx.user.id,workspaceId,kind:"token_checkout",title:"Checkout token dibuat",message:`Order ${orderCode} untuk ${pkg.tokens} token senilai ${money(pkg.price)} sudah dibuat. Selesaikan pembayaran paling lambat ${expiryText} WIB. Jika belum dibayar sampai batas tersebut, order otomatis dibatalkan.`,actionUrl:x.payment_link_url||`${origin}/#billing`});return NextResponse.json({ok:true,order_code:orderCode,payment_url:x.payment_link_url,payment_session_id:x.payment_session_id,expires_at:providerExpiry,channels:x.allowed_payment_channels||[],notification_delivery:delivery});} catch(error:any){if(ctx){try{await ctx.admin.from("luma_api_usage_events").insert({workspace_id:workspaceId||null,user_id:ctx.user.id,provider:"xendit",service:"payment_session",request_type:"token_checkout",status:"error",reference:orderCode||null,metadata:{error:error?.message||"unknown"}})}catch{}}return NextResponse.json({ok:false,error:"error, terjadi kesalahan."},{status:400});}}
