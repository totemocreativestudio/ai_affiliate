import { randomUUID } from "crypto";
import { NextRequest,NextResponse } from "next/server";
import { getServerContext } from "../../../../lib/server-auth";
import { getServerSecret } from "../../../../lib/server-secrets";
import { sendExternalCustomerNotice } from "../../../../lib/customer-notifications";
import { resolvePromo } from "../../../../lib/promo";
export const runtime="nodejs";
const money=(v:any)=>new Intl.NumberFormat("id-ID",{style:"currency",currency:"IDR",maximumFractionDigits:0}).format(Number(v||0));
export async function POST(req:NextRequest){let ctx:any=null;try{const body=await req.json();const workspaceId=String(body.workspace_id||"");const planId=Number(body.plan_id||0);ctx=await getServerContext(workspaceId);const {data:plan,error}=await ctx.admin.from("luma_subscription_plans").select("*").eq("id",planId).eq("status","active").single();if(error||!plan||plan.is_trial)throw new Error();let promo:any=null,discount=0;if(body.promo_code){const p=await resolvePromo(ctx.admin,ctx.user.id,String(body.promo_code),"subscription",plan.code,Number(plan.price));promo=p.promo;discount=p.effect.discount_amount}const amount=Math.max(0,Number(plan.price)-discount);const orderCode=`SUB-${randomUUID().replace(/-/g,"").slice(0,12).toUpperCase()}`;const origin=(process.env.NEXT_PUBLIC_APP_URL||new URL(req.url).origin).replace(/\/$/,"");const expiresAt=new Date(Date.now()+3*86400000).toISOString();if(amount<=0){await ctx.admin.from("luma_subscription_orders").insert({user_id:ctx.user.id,workspace_id:workspaceId,plan_id:plan.id,order_code:orderCode,base_amount:plan.price,discount_amount:discount,amount:0,status:"pending",payment_provider:"Promo",payment_method:"Promo 100%",promo_id:promo?.id||null,expires_at:expiresAt});const {data:result,error:completeError}=await ctx.admin.rpc("luma_complete_subscription",{p_order_code:orderCode,p_payment_reference:`PROMO-${promo?.code||"FREE"}`,p_provider_payload:{promo_code:promo?.code||null}});if(completeError)throw completeError;return NextResponse.json({ok:true,free:true,result});}const key=await getServerSecret(ctx.admin,"luma_xendit_secret_key");if(!key)throw new Error();const payload:any={reference_id:orderCode,session_type:"PAY",mode:"PAYMENT_LINK",amount,currency:"IDR",country:"ID",locale:"id",expires_at:expiresAt,description:`Lumaway ${plan.name}`,customer:{reference_id:`luma-${ctx.user.id}-${Date.now()}`,type:"INDIVIDUAL",email:ctx.user.email||undefined},items:[{reference_id:`subscription-${plan.code}`,name:`Lumaway ${plan.name}`,description:`Akses Lumaway ${plan.duration_days} hari`,type:"DIGITAL_SERVICE",category:"SOFTWARE",net_unit_amount:amount,quantity:1,currency:"IDR"}],metadata:{workspace_id:workspaceId,user_id:ctx.user.id,plan_code:plan.code}};if(origin.startsWith("https://")){payload.success_return_url=`${origin}/#billing`;payload.cancel_return_url=`${origin}/#billing`}const response=await fetch("https://api.xendit.co/sessions",{method:"POST",headers:{Authorization:`Basic ${Buffer.from(`${key}:`).toString("base64")}`,"Content-Type":"application/json"},body:JSON.stringify(payload)});const x=await response.json();if(!response.ok)throw new Error();const providerExpiry=String(x.expires_at||expiresAt);await ctx.admin.from("luma_subscription_orders").insert({user_id:ctx.user.id,workspace_id:workspaceId,plan_id:plan.id,order_code:orderCode,base_amount:plan.price,discount_amount:discount,amount,status:"pending",payment_provider:"Xendit",payment_method:"Secure Checkout",payment_session_id:x.payment_session_id||null,payment_url:x.payment_link_url||null,promo_id:promo?.id||null,expires_at:providerExpiry,provider_payload:{status:x.status,promo_code:promo?.code||null}});const expiryText=new Date(providerExpiry).toLocaleString("id-ID",{timeZone:"Asia/Jakarta",dateStyle:"medium",timeStyle:"short"});await sendExternalCustomerNotice(ctx.admin,{userId:ctx.user.id,workspaceId,kind:"subscription_checkout",title:"Checkout langganan dibuat",message:`Paket ${plan.name} senilai ${money(amount)} sudah dibuat. Selesaikan pembayaran paling lambat ${expiryText} WIB.`,actionUrl:x.payment_link_url||`${origin}/#billing`});return NextResponse.json({ok:true,order_code:orderCode,payment_url:x.payment_link_url,expires_at:providerExpiry,amount,discount_amount:discount});}catch{return NextResponse.json({ok:false,error:"error, terjadi kesalahan."},{status:400})}}
